import { fetchNotifications } from "../../../firebase-api/firebase-api";
import classes from "./BattleLogNotifications.module.css";

const BattleLogNotifications = (props) => {
  return <div className={classes.notificationsContainer}></div>;
};

export default BattleLogNotifications;
